import {
  StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: 45,
    backgroundColor: '#FFF',
    marginBottom: 5,
    marginLeft: 8,
    marginRight: 8,
    shadowColor: '#000',
    shadowOpacity: 0.3,
    shadowOffset: {
      width: 0,
      height: 2
    },
    borderRadius: 2,
    borderWidth: 0.5,
    borderColor: '#d6d7da',
  }
});

export default styles;
